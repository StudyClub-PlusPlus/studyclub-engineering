[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
# 수동 테스트 하니스(tests/manual)는 pytest 대상이 아니다.
norecursedirs = manual
filterwarnings =
    ignore::DeprecationWarning
