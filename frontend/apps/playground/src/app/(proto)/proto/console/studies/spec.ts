import type { ScreenSpec } from '@/proto/annotate';

/**
 * 스터디 목록 — Story 별 명세.
 *
 * **번호는 Story 마다 1 부터 다시 매긴다.** 근거는 `@console/components/StudiesTable.tsx` 다.
 */

/** 스터디 공개 — 등록해 둔 스터디를 사이트에 세운다. */
export const PUBLISH_SPEC: ScreenSpec = {
  screen: '스터디 관리 (운영 콘솔)',
  chip: '스터디 공개',
  scope: 'publish',
  notes: [
    '공개 전환은 화면 상태로만 처리한다. 새로고침하면 되돌아간다 — 저장 API 가 아직 없다',
    '등록·수정·삭제는 이 화면 범위 밖이다 (각각 별도 Story)',
  ],
  entries: [
    {
      n: '1',
      title: '공개 설정',
      display: ['이 스터디가 사용자 사이트에 서 있는가 — 「공개」 · 「미공개」'],
      behavior: [
        '누르면 그 자리에서 바뀐다 — 저장 조작이 따로 없다',
        '공개 → 미공개도 같은 자리에서 한다. 내리는 길을 따로 두지 않는다',
      ],
      policy: [
        '**공개는 모집 시작 일자가 있는가로 판단한다.** 「공개」를 켜는 것이 곧 모집 시작이다 — 스터디 상태가 「개설」(`OPEN`)이 되고 시작 일자가 채워진다',
        '**등록 직후는 늘 미공개다.** 공개는 사람이 켠다',
        '예약 공개(그날이 되면 저절로 열림)는 두지 않는다 — 준비가 됐는지는 날짜가 판단할 수 없다',
        '두 상태가 서로 구분되어야 하며, 색만으로 구분하지 않는다',
      ],
      data: ['스터디의 공개 여부 (쓰기)'],
    },
    {
      n: '1-1',
      title: '공개할 수 없는 상태',
      when: '신청 폼이 연결되지 않았을 때',
      display: ['누를 수 없는 자리임이 드러나고, 짚어 보면 이유를 알려준다 — 「신청 폼을 먼저 연결하세요.」'],
      policy: [
        '**신청 폼이 없으면 공개하지 못한다** — 공개하는 순간 크루가 신청할 데 없는 스터디를 보게 된다',
        '반대 방향(공개 → 미공개)은 언제나 된다. 되돌리는 길까지 막으면 공개를 누르기가 무서워진다',
      ],
    },
    {
      n: '2',
      title: '신청 폼',
      display: ['그 스터디에 신청 폼이 걸려 있는가'],
      policy: [
        '공개 설정 바로 왼쪽에 둔다 — 공개를 켜기 전에 확인하는 값이고, 켠 뒤에는 볼 일이 없다',
        '**모집중인데 폼이 없을 때만** 눈에 띄게 한다. 마감된 스터디의 빈 폼은 이미 지난 일이라 고칠 것이 없다',
        '여기서 폼을 붙이지 않는다 — 붙이는 자리는 스터디 운영 화면이다 (별도 Story)',
      ],
      data: ['스터디의 신청 폼 주소 (읽기)'],
    },
    {
      n: '4',
      title: '공개 확인 팝업',
      when: '공개 상태 칸을 눌렀을 때',
      display: [
        '공개할 때 「‘스터디명’ 스터디를 공개하시겠습니까?」 — 오늘이 모집 시작일이 되고 스터디 상태가 「개설」이 되어 사이트에 노출된다는 안내',
        '내릴 때 「비공개로 전환하시겠습니까?」 — 사이트에서 내려가고 상태가 「작성 중」으로 돌아간다는 안내',
      ],
      behavior: ['「공개」 / 「비공개로 전환」을 눌러야 바뀐다. 「취소」·배경 클릭·Esc 는 아무것도 바꾸지 않는다'],
      policy: ['**누르면 바로 바뀌지 않는다** — 공개는 모집 시작이라 실수로 눌러도 사이트에 나간다'],
    },
    {
      n: '3',
      title: '공개 필터',
      display: ['드롭다운 「공개 전체」 · 「공개」 · 「미공개」'],
      behavior: ['고른 상태만 남는다'],
      policy: ['미공개만 모아 보는 길이 있어야 한다 — 등록해 두고 잊은 스터디가 여기 쌓인다'],
    },
  ],
};

/** 스터디 목록 — 기획 8개 항목이 목록에 어떻게 보이는가. */
export const LIST_SPEC: ScreenSpec = {
  screen: '스터디 관리 (운영 콘솔)',
  chip: '스터디 목록',
  scope: 'list',
  entries: [
    {
      n: '1',
      title: '지원 현황',
      display: ['열 이름은 「지원 현황」. 「현재/정원」, 정원을 비운 스터디는 「현재 · 제한 없음」'],
      policy: [
        '현재 인원은 신청 명부에서 계산한다 — 운영자가 입력하지 않는다',
        '정원은 등록 폼의 「모집 정원」이다. 모집 회차마다 따로 정한다',
        '제목 · 주제 · 종류 · 시간대 · 모집 상태와 마감일 · 스터디 시작일 · 공개 상태가 같은 행에 함께 보인다',
      ],
      data: ['study.recruitment.capacity', '신청 명부의 참여자 수'],
    },
    {
      n: '2',
      title: '모집 시작일',
      display: ['모집 상태(모집중 · 마감)와 **다른 열**이다. 모집을 시작한 날, 시작 전이면 「—」'],
      policy: ['**공개한 날이다** — 모집 시작 일자가 있으면 공개로 본다. 별도 「공개 시작일」 열은 두지 않는다'],
      data: ['STUDY_RECRUITMENT.START_AT'],
    },
    {
      n: '2-1',
      title: '모집 종료일',
      display: ['모집 마감일. 모집 시작일과 **별개의 열**이다'],
      policy: ['필수 입력이다 — 상시 모집은 없다. 이 날이 지나면 모집 마감이다'],
      data: ['STUDY_RECRUITMENT.RECRUIT_DEADLINE_AT'],
    },
    {
      n: '2-2',
      title: '종류',
      display: ['「스터디」 · 「클럽」'],
      policy: [
        '기수(이 목록의 한 행)가 아니라 **프로그램의 속성**이다 — 같은 프로그램의 기수는 전부 같은 값을 갖는다',
        '새 프로그램을 등록할 때 정하고, 그 뒤로는 바꾸지 못한다',
      ],
      data: ['STUDY_PROGRAM.STUDY_KIND'],
    },
    {
      n: '2-3',
      title: '시간대',
      display: ['「KST」 · 「PST」 · 「동시 진행(KST·PST)」. 값이 없으면 「미정」'],
      policy: [
        '등록 폼에서 운영자가 직접 고른 값이다 — 일정·킥오프 문구를 읽어서 추정하지 않는다',
        '이 필드가 생기기 전에 만든 스터디는 값이 없어 「미정」으로 보인다. 사용자 사이트도 이 값이 있으면 그대로 쓰고, 없는 행만 문구로 추정한다',
      ],
      data: ['STUDY.TIMEZONE'],
    },
    {
      n: '3',
      title: '스터디 시작일',
      display: ['모임이 실제로 시작하는 날. 등록 폼의 「진행 시작일」과 같은 값이다. 없으면 「미정」'],
      policy: [
        '모집 시작일·모집 종료일과는 다른 축이다 — 저 둘은 「언제까지 신청받는가」, 이건 「언제 모이기 시작하는가」',
        '자유 텍스트인 「진행 일정」(요일·시간 문구)은 목록에 두지 않는다 — 정렬·필터를 걸 수 없고, 운영자가 훑어볼 값이 아니라 신청 화면에 알리는 문구다',
      ],
      data: ['STUDY.START_AT'],
    },
    {
      n: '4',
      title: '필터',
      display: ['검색 · 주제 · 상태 · 종류 · 모집 · 공개 드롭다운과 「신청 폼 없는 것만」 토글, 개수를 **한 줄**에 나란히'],
      policy: [
        '두 줄로 나누지 않는다. 화면이 좁으면 줄바꿈 대신 옆으로 넘긴다',
        '축이 많아 탭으로 늘어놓으면 장황해진다 — 드롭다운으로 줄인다. 「전체」 항목에 축 이름을 붙여 라벨 없이도 구분한다',
        '주제 필터는 드롭다운 단일 선택이다 — `STUDY.CATEGORY` 가 단일 값이라 목록도 한 번에 하나만 고른다',
        '정렬은 필터 줄이 아니라 컬럼 머리의 화살표로 한다',
      ],
    },
    {
      n: '5',
      title: '신청 폼 필터',
      display: ['토글 「신청 폼 없는 것만」'],
      behavior: ['켜면 신청 폼이 없는 스터디만 남는다. 끄면 전체'],
      policy: ['모집중인데 폼이 없는 스터디를 모아 보는 길이다 — 신청할 데가 없는 채로 공고가 나가는 것을 막는다'],
    },
    {
      n: '6',
      title: '컬럼 순서 변경',
      display: ['열 머리에 끌기 손잡이', '순서가 기본과 다르면 「컬럼 순서 초기화」가 나타난다'],
      behavior: ['열 머리를 끌어다 놓아 옮긴다', '바꾼 순서는 이 브라우저에 저장된다'],
      policy: ['**스터디(제목) 열은 맨 앞에 고정**한다 — 행이 어느 스터디인지 잃으면 나머지를 읽을 수 없다'],
    },
    {
      n: '7',
      title: '가로 스크롤바 위치',
      display: ['표 위에 가로 스크롤바가 하나 더 있다. 아래 스크롤바와 함께 움직인다'],
      policy: [
        '열이 많아 가로로 넘치는데, 행이 많으면 표 맨 아래까지 내려가야 스크롤바가 보인다 — 위에서도 넘길 수 있게 한다',
      ],
    },
    {
      n: '8',
      title: '페이징',
      display: [
        '표 아래 페이지 번호. 한 페이지 20개. 「이전」 · 「다음」 과 번호, 번호가 많으면 앞뒤를 「…」 로 줄인다',
      ],
      behavior: ['필터·검색·정렬을 바꾸면 1페이지로 돌아간다', '한 페이지에 다 들어가면 페이지 번호를 보이지 않는다'],
      policy: ['상단의 「N개」 는 페이지가 아니라 필터에 걸린 전체 개수다'],
    },
    {
      n: '9',
      title: '컬럼 정렬',
      display: ['컬럼 머리 이름 옆에 위·아래 작은 화살표. 눌러 둔 화살표는 강조색'],
      behavior: [
        '위 화살표 = 오름차순, 아래 화살표 = 내림차순. 누르면 바로 정렬된다',
        '이미 눌린 화살표를 다시 누르면 정렬이 풀리고 기본 순서로 돌아간다',
        '한 번에 한 열로만 정렬한다. 다른 열 화살표를 누르면 옮겨 간다',
        '정렬하면 1페이지로 돌아간다',
      ],
      policy: [
        '기본 순서는 모집중 먼저, 그 안에서 마감이 임박한 순이다 — 운영자가 손댈 것부터 위로',
        '값이 없는 행(예: 모집 시작일이 없는 미공개)은 오름·내림과 관계없이 맨 뒤로 보낸다',
        '프로그램 · 스터디 · 상태 · 주제 · 종류 · 시간대 · 모집 시작일 · 모집 종료일 · 모집 상태 · 지원 현황 · 스터디 시작일 · 출석률 · 신청 폼 · 공개 상태 모두 정렬된다',
      ],
    },
  ],
};

/** 스터디 상태 — 목록 열·필터·안내. */
export const STATUS_SPEC: ScreenSpec = {
  screen: '스터디 관리 (운영 콘솔)',
  chip: '스터디 상태',
  scope: 'status',
  entries: [
    {
      n: '1',
      title: '스터디 상태 안내',
      display: [
        '목록 위 한 줄. 다섯 단계 배지를 순서대로 — 작성 중(DRAFT) → 개설(OPEN) → 진행 중(ONGOING) → 종료(ENDED) → 운영 종료(CLOSED)',
        '끝에 「모집중/마감은 상태가 아니라 모집 시작일·종료일과 정원으로 계산합니다」 한 줄',
      ],
      behavior: ['배지에 마우스를 올리면 그 단계의 뜻과 다음 단계로 넘기는 주체가 뜬다'],
      policy: [
        '**단계 이름에 「공개」를 쓰지 않는다** — 진행 중인 스터디도 공개 상태다. 공개 여부는 「공개 상태」 열이, 신청 가능 여부는 「모집중/마감」이 말한다. 단계는 그 둘과 다른 사실(어디까지 진행됐는가)만 말한다',
        '작성 중 → 개설: 캡틴이 공개하며 모집 시작. 개설 → 진행 중: 네비게이터가 첫 미팅 등록',
        '진행 중 → 종료: 네비게이터 종료 처리, 또는 마지막 미팅으로부터 N주 경과 (N 미정)',
        '종료 → 운영 종료: 캡틴이 채널을 삭제하고 운영 종료',
      ],
      data: ['STUDY.STATUS'],
    },
    {
      n: '2',
      title: '스터디 상태 열',
      display: ['상태 배지. 모집 상태 배지(모집중 · 마감)와 열이 따로 있다'],
      policy: ['미공개 스터디는 늘 「작성 중」이다. 상태는 사람이 하는 일로만 바뀐다 — 날짜가 지났다고 바뀌지 않는다'],
      data: ['STUDY.STATUS'],
    },
    {
      n: '3',
      title: '상태 필터',
      display: ['드롭다운 「상태 전체」 + 다섯 상태'],
      behavior: ['고른 상태의 스터디만 남는다'],
    },
  ],
};

/** 스터디 프로그램 ID 열 — 그룹화는 하지 않고 컬럼별 정렬만 한다. */
export const PROGRAM_SPEC: ScreenSpec = {
  screen: '스터디 관리 (운영 콘솔)',
  chip: '프로그램 ID',
  scope: 'program',
  notes: [
    '프로그램은 스터디의 정체성이고 기수(스터디)가 그 아래 쌓인다. 프로토는 제목에서 기수 표기를 뗀 값으로 프로그램 ID 를 매긴다 — 실제 값이 아니다',
    '프로그램별로 묶어 보여주지 않는다 — 같은 프로그램을 모아 보고 싶으면 P-ID 열을 정렬한다',
  ],
  entries: [
    {
      n: '1',
      title: '프로그램 ID (열 이름 P-ID)',
      display: [
        '열 이름은 「P-ID」. 열 하나. 같은 프로그램의 기수는 같은 ID 를 갖는다. 프로그램 제목 열은 두지 않는다',
      ],
      policy: [
        '**P-ID › 스터디 순으로 맨 앞 두 칸에 고정**한다 — 프로그램이 스터디를 묶는 위계다. 이 두 칸은 순서를 바꿀 수 없다',
        '스터디 열은 넓게 잡는다 — 제목이 긴 쪽은 스터디다',
      ],
      data: ['STUDY_PROGRAM.ID'],
    },
    {
      n: '2',
      title: 'P-ID 정렬',
      display: ['P-ID 열 머리의 위·아래 화살표'],
      behavior: ['다른 열과 같은 방식으로 정렬한다 — 컬럼 정렬 참조'],
      policy: ['머리 줄·그룹 구분 없이 행을 그대로 나열한다'],
    },
  ],
};

export const SPECS = [PUBLISH_SPEC, LIST_SPEC, STATUS_SPEC, PROGRAM_SPEC];
