import type { ScreenSpec } from '@/proto/annotate';

/**
 * 스터디 운영 화면 — Story 별 번호 명세.
 *
 * 한 지면에 Story 가 여럿 산다. **번호는 Story 마다 1 부터 다시 매긴다.** 요소에는
 * `data-anno="<scope>:<n>"` 으로 달고, 한 요소가 여러 Story 에 나오면 공백으로 나열한다.
 *
 * Story ID 는 발번 대장에서 실제로 받기 전까지 비워 둔다.
 */

/** 참석자 목록 — 지금 누가 이 스터디에 참여하고 있는가. */
export const ATTENDEE_SPEC: ScreenSpec = {
  screen: '스터디 운영 (운영 콘솔)',
  chip: '참석자 목록',
  scope: 'attendee',
  notes: [
    '신청 승인·내보내기는 별도 Story 소관이다 — 여기서는 명단을 보는 것까지',
    '회차별 출석 여부는 출석 탭에서 본다',
  ],
  entries: [
    {
      n: '1',
      title: '스터디 헤더',
      display: ['어느 스터디의 명단을 보고 있는지 알려준다'],
    },
    {
      n: '1-1',
      title: '스터디명',
      data: ['스터디 제목 (운영 콘솔은 한국어 고정)'],
    },
    {
      n: '1-2',
      title: '모집 상태 · 공개 예정',
      display: [
        '「모집중 · D-5」 · 「오늘 마감」 · 「상시 모집」 · 「모집 마감」 중 하나',
        '공개일이 아직 오지 않았으면 「공개 예정」이 함께 붙는다',
      ],
      policy: [
        '헤더는 상태만 말한다 — 마감일·공개일의 실제 날짜는 정보 탭이 갖는다',
        '모집 상태는 recruitState() 한 곳에서만 계산한다 — 사용자 사이트와 판정이 갈리면 안 된다',
      ],
    },
    {
      n: '2',
      title: '탭',
      display: ['정보 · 신청자 · 출석. 참석자 명단은 신청자 탭에 있다'],
      behavior: ['신청자 탭에는 승인 대기가 있으면 배지가 붙는다'],
    },
    {
      n: '3',
      title: '참여 크루 명단',
      when: '신청자 탭을 보고 있을 때',
      display: ['승인된 크루만 나온다. 이름 · 이메일 · 지역 · 완주율'],
      policy: [
        '승인 대기는 이 명단에 섞지 않는다 — 지금 참여 중인 사람이 몇 명인지가 흐려진다',
        '완주율은 지난 스터디 이력이다. 이력이 없으면 숫자를 만들지 않고 「첫 참여」로 적는다',
      ],
      data: ['승인(active) 상태 참여 건'],
    },
    {
      n: '3-1',
      title: '참여 인원 / 정원',
      when: '신청자 탭을 보고 있을 때',
      display: ['「참여 크루 8/12」처럼 승인된 인원과 정원'],
      policy: ['승인(active)만 센다. 승인 대기는 빠진다'],
    },
  ],
};

/** 신청자 승인 — 누가 들어오는가. */
export const CREW_SPEC: ScreenSpec = {
  screen: '스터디 운영 (운영 콘솔)',
  chip: '신청자 승인',
  scope: 'crew',
  notes: ['승인은 화면 상태로만 처리한다. 새로고침하면 되돌아간다 — 저장 API 가 아직 없다'],
  entries: [
    {
      n: '1',
      title: '탭',
      behavior: ['신청자 탭에 승인 대기가 있으면 숫자 배지가 붙는다 — 오늘 처리할 일이 있다는 신호'],
    },
    {
      n: '2',
      title: '승인 대기',
      when: '신청자 탭을 보고 있을 때',
      display: ['이름 · 이메일 · 지역 · 완주율 · 신청일. 처리할 신청이 없으면 그 사실을 적는다'],
      behavior: ['「승인」을 누르면 참여 크루 명단으로 내려간다'],
      policy: [
        '승인 대기를 참여 명단 위에 따로 뺀다 — 섞어 두면 오늘 처리할 일이 몇 건인지 세어야 한다',
        '버튼은 「승인」 하나뿐이다. 승인하지 않고 남겨 둔 것이 곧 미승인이다',
      ],
    },
  ],
};

/** 스터디 수정·삭제 — 정보 탭에서 한다. */
export const EDIT_SPEC: ScreenSpec = {
  screen: '스터디 운영 (운영 콘솔)',
  chip: '스터디 수정·삭제',
  scope: 'edit',
  notes: [
    '저장·삭제는 화면 상태로만 처리한다. 새로고침하면 되돌아간다 — 저장 API 가 아직 없다',
    '항목별 입력 규칙은 스터디 등록 Story 소관이다',
  ],
  entries: [
    {
      n: '1',
      title: '정보 탭 본문 (입력 폼)',
      when: '정보 탭을 보고 있을 때',
      display: ['등록 팝업과 같은 항목이 등록된 값으로 채워져 열린다'],
      policy: [
        '보이는 칸이 곧 고치는 칸이다 — 읽기 화면과 수정 팝업을 따로 두지 않는다',
        '모집 마감일·공개일의 실제 날짜는 이 폼이 갖는다. 헤더는 상태만 말한다',
      ],
    },
    {
      n: '2',
      title: '저장',
      when: '정보 탭을 보고 있을 때',
      behavior: [
        '검증 통과 → 저장하고 「저장되었습니다.」 표시',
        '검증 실패 → 해당 칸에 오류 표시, 저장 안 함',
        '처리 중 → 버튼 비활성 (중복 제출 차단)',
      ],
      policy: ['검증 규칙은 등록 폼과 같다'],
    },
    {
      n: '3',
      title: '스터디 삭제',
      when: '정보 탭을 보고 있을 때',
      behavior: ['누르면 확인 창이 뜬다. 그 자리에서 지워지지 않는다'],
      policy: ['저장과 반대편 끝에 둔다 — 잘못 누르면 되돌릴 수 없다'],
    },
    {
      n: '4',
      title: '삭제 확인 창',
      selector: '[role="dialog"]',
      when: '「스터디 삭제」를 눌렀을 때',
      display: ['삭제 대상 스터디명', '함께 사라지는 것: 크루 명단 · 출석 기록'],
      behavior: ['취소 · 배경 클릭 · Esc → 아무것도 지우지 않고 닫힘'],
    },
  ],
};

export const SPECS = [ATTENDEE_SPEC, CREW_SPEC, EDIT_SPEC];
