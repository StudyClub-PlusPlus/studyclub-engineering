import type { ScreenSpec } from '@/proto/annotate';

/**
 * 유저 — 번호별 명세.
 *
 * 근거는 `@console/lib/roles.ts`(역할·권한 모델)와 `@console/components/UsersTable.tsx` 다.
 * Story ID 는 발번 대장에서 실제로 받기 전까지 비워 둔다.
 */
export const SPEC: ScreenSpec = {
  screen: '유저 (운영 콘솔)',
  notes: [
    '역할·권한 변경은 화면 상태로만 처리한다. 새로고침하면 되돌아간다 — 저장 API 가 아직 없다',
    '탈퇴·정지·초대는 이 화면 범위 밖이다 (PRD 에서 다시 다룬다)',
    '역할 변경 이력(누가 언제 무엇을 바꿨는가)은 아직 없다 — PRD 오픈 이슈',
    '역할 밖의 개별 권한 예외는 두지 않는다. 줄 수 있는 것은 역할뿐이다',
    '백엔드 Role enum(STUDENT/OPERATOR/ADMIN)과 이름이 다르다 — PRD 에서 정렬한다',
  ],
  entries: [
    {
      n: '1',
      title: '페이지 헤더',
      display: ['제목 「유저」'],
    },
    {
      n: '2',
      title: 'ⓘ 역할별 기본 권한',
      behavior: ['누르면 역할별 기본 권한표 모달이 열린다'],
      policy: ['표를 화면에 펴 두지 않는다 — 명단은 매번 보고 역할 체계는 가끔 본다'],
    },
    {
      n: '3',
      title: '역할별 기본 권한 표 (모달)',
      selector: '[role="dialog"]',
      when: 'ⓘ 를 눌렀을 때',
      display: ['행=권한 10종, 열=역할 3종(캡틴·네비게이터·크루). 허용은 체크, 없음은 −'],
      data: ['`ROLE_PERMISSIONS` 한 곳에서만 온다'],
      policy: [
        '열 순서는 권한이 많은 쪽부터다 (캡틴 → 네비게이터 → 크루)',
        '「역할 부여」는 캡틴 전용이다',
        '네비게이터는 맡은 스터디를 굴리는 데 필요한 것만 갖는다 — 알럿·출석·정보 수정·명단 열람',
        '신청 승인·내보내기는 캡틴 전용이다. 누가 들어오는지는 캡틴이 정한다',
      ],
    },
    {
      n: '4',
      title: '역할 탭 · 검색 · 인원 수',
      display: ['탭 = 전체 · 캡틴 · 네비게이터 · 크루', '오른쪽 끝에 「총 N명」'],
      behavior: [
        '탭을 누르면 그 역할만 남는다',
        '검색은 이름과 이메일에 걸린다',
        '「총 N명」은 탭·검색으로 걸러진 뒤의 수다 — 지금 장의 인원이 아니다',
      ],
      policy: [
        '역할이 셋뿐이라 접지 않는다 — 펴 두면 지금 무엇으로 걸러져 있는지 한눈에 보인다',
        '역할 순서는 화면 어디서나 같다: 캡틴 → 네비게이터 → 크루 (`ROLES` 가 소유)',
      ],
    },
    {
      n: '5',
      title: '유저 목록',
      display: [
        '이름(참여 스터디가 없으면 「휴면」) · 이메일 · 역할 · 참여 스터디 수 · 가입일',
        '역할은 색 배지 (캡틴=브랜드 · 네비게이터=정보 · 크루=중립)',
      ],
      data: ['스터디 크루 mock 을 이메일로 합쳐 만든 명단 (`consoleUsers`)'],
      behavior: [
        '역할 배지를 누르면 역할 목록이 뜨고, 고르면 그 자리에서 바로 바뀐다 — 별도 저장 버튼이 없다',
        '바꿀 수 없는 줄은 배지가 흐려지고 화살표가 없다. 마우스를 올리면 이유가 뜬다 (자기 자신 · 마지막 캡틴)',
      ],
      policy: [
        '정렬은 캡틴 → 네비게이터 → 크루, 그 안에서 참여 스터디가 많은 순',
        '이 화면을 여는 사람은 캡틴이다 — 「캡틴인가」를 화면에서 다시 묻지 않는다',
        '자기 역할은 스스로 못 바꾼다. 마지막 캡틴은 내릴 수 없다 — 되돌릴 사람이 사라진다',
      ],
    },
    {
      n: '6',
      title: '페이지네이션',
      display: ['이전 · 페이지 번호(최대 7칸, 잘리는 자리는 「…」) · 다음. 한 장에 20명'],
      behavior: ['탭이나 검색을 바꾸면 1장으로 돌아간다', '걸러진 결과가 한 장에 들어가면 아예 표시하지 않는다'],
      policy: ['현재 장은 배경이 채워진다 — 색만으로 구분하지 않도록 굵기도 함께 바뀐다'],
    },
  ],
};
