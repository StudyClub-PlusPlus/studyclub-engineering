import type { ScreenSpec } from '@/proto/annotate';

/**
 * 유저 — 번호별 명세.
 *
 * 한 지면에 Story 둘이 산다. **번호는 Story 마다 1 부터 다시 매긴다.**
 * 근거는 `@console/lib/roles.ts`(역할·권한 모델)와 `@console/components/UsersTable.tsx` 다.
 */

/** 전체 유저 목록 — 누가 있는가. */
export const LIST_SPEC: ScreenSpec = {
  screen: '유저 (운영 콘솔)',
  chip: '유저 목록',
  scope: 'list',
  notes: [
    '**탈퇴한 회원은 이 목록에 없다.** 처리방침 제4조가 회원 정보의 보유 기간을 「회원 탈퇴 시까지」로 두므로, 목록에 남기려면 파기해야 할 정보를 계속 들고 있어야 한다',
    '탈퇴 회원이 참여했던 회차의 출석 기록은 남는다 — 다른 참여자의 기록과 결합되어 있어 빼면 그 회차의 출석률이 깨진다. 다만 누구인지는 알 수 없게 처리한다 (이용약관 제11조 3항)',
    '정지·초대는 이 화면 범위 밖이다',
  ],
  entries: [
    {
      n: '1',
      title: '페이지 헤더',
      display: ['제목 「유저」'],
    },
    {
      n: '2',
      title: '역할 탭 · 검색 · 인원 수',
      display: ['탭 = 전체 · 캡틴 · 네비게이터 · 크루', '걸러진 뒤의 인원 수'],
      behavior: [
        '탭을 고르면 그 사람들만 남는다. 「네비게이터」는 담당 스터디가 있는 사람이다',
        '검색은 이름과 이메일에 걸린다',
        '인원 수는 탭·검색으로 걸러진 뒤의 수다 — 지금 장의 인원이 아니다',
      ],
      policy: [
        '역할이 셋뿐이라 접지 않는다 — 지금 무엇으로 걸러져 있는지 한눈에 보여야 한다',
        '역할 순서는 화면 어디서나 같다: 캡틴 → 네비게이터 → 크루',
      ],
    },
    {
      n: '3',
      title: '유저 목록',
      display: ['이름(참여 스터디가 없으면 「휴면」) · 이메일 · 권한 · 네비게이터 · 가입일'],
      data: ['스터디 크루를 이메일로 합쳐 만든 명단'],
      policy: [
        '정렬은 캡틴 → 담당 스터디가 있는 사람 → 나머지, 그 안에서 참여 스터디가 많은 순',
        '**네비게이터 칸은 읽기 전용이다** — 누가 어느 스터디를 맡는지는 그 스터디의 크루 명단에서 정한다',
        '네비게이터 칸에는 담당 스터디 이름을 적는다. 둘 이상이면 하나만 적고 나머지는 개수로 접는다',
      ],
    },
    {
      n: '4',
      title: '페이지 이동',
      display: ['한 장에 20명. 현재 장이 어디인지 드러난다'],
      behavior: ['탭이나 검색을 바꾸면 첫 장으로 돌아간다', '한 장에 다 들어가면 아예 나오지 않는다'],
      policy: ['현재 장은 색만으로 구분하지 않는다'],
    },
  ],
};

/** 권한 부여 — 계정에 캡틴을 주거나 거둔다. */
export const ROLE_SPEC: ScreenSpec = {
  screen: '유저 (운영 콘솔)',
  chip: '권한 부여',
  scope: 'role',
  notes: [
    '권한 변경은 화면 상태로만 처리한다. 새로고침하면 되돌아간다 — 저장 API 가 아직 없다',
    '**계정에 줄 수 있는 것은 캡틴·크루 둘뿐이다.** 네비게이터는 스터디마다 서는 역할이라 그 스터디의 크루 명단에서 정한다',
    '역할 밖의 개별 권한 예외는 두지 않는다',
    '백엔드 Role enum(STUDENT/OPERATOR/ADMIN)과 이름이 다르다 — 매핑은 PRD 에서 정한다',
  ],
  entries: [
    {
      n: '1',
      title: '현재 권한',
      display: [
        '그 사람의 지금 권한. 바꿀 수 있는 자리임이 드러난다',
        '바꿀 수 없는 줄은 바꿀 수 있는 줄과 구분되고, 짚어 보면 이유를 알려준다',
      ],
      behavior: ['고르면 고를 수 있는 값이 열린다'],
      policy: ['두 값이 서로 구분되어야 하며, 색만으로 구분하지 않는다'],
      data: ['계정 권한 값'],
    },
    {
      n: '2',
      title: '권한 고르기',
      when: '현재 권한을 눌렀을 때',
      display: ['고를 수 있는 값 둘 — 캡틴 · 크루'],
      behavior: ['고르면 그 자리에서 바로 바뀐다 — 저장 조작이 따로 없다', '바깥을 누르거나 Esc → 바꾸지 않고 닫힌다'],
      policy: [
        '순서는 캡틴 → 크루. 화면 어디서나 같다',
        '**지금 값을 따로 표시하지 않는다** — 닫힌 자리에 이미 보이므로 목록 안에서 또 말하면 같은 말이 두 번이다',
        '자기 권한은 스스로 바꿀 수 없다',
        '마지막 캡틴은 내릴 수 없다 — 되돌릴 사람이 사라진다',
      ],
      data: ['계정 권한 값 (쓰기)'],
    },
    {
      n: '3',
      title: '역할별 기본 권한 열기',
      behavior: ['누르면 역할별 기본 권한표가 열린다'],
      policy: ['표를 화면에 펴 두지 않는다 — 명단은 매번 보고 역할 체계는 가끔 본다'],
    },
    {
      n: '4',
      title: '역할별 기본 권한 표',
      selector: '[role="dialog"]',
      when: '3 을 눌렀을 때',
      display: [
        '**표가 둘이다** — 스터디 단위 권한(캡틴 · 네비게이터 · 크루) 넷, 사이트 전체 권한(캡틴 · 크루) 일곱',
        '첫 칸 머리는 「하는 일」. 스터디 단위 표 제목 옆에 「네비게이터 권한은 담당 스터디에 국한」',
      ],
      behavior: ['열람 전용이다. 이 표에서 권한을 바꾸지 않는다'],
      policy: [
        '**담당 스터디 안에서 도는 일과 사이트 전체에 미치는 일을 한 표에 섞지 않는다** — 섞으면 네비게이터의 체크가 모든 스터디를 뜻하는 것처럼 읽힌다',
        '역할 순서는 권한이 많은 쪽부터 (캡틴 → 네비게이터 → 크루)',
        '**출석 체크는 권한이 아니다** — 보이스룸에서 그날 출석을 여는 일은 누구나 할 수 있다. 권한이 필요한 것은 기록된 출석을 고치는 일이다',
        '**스터디 공개는 정보 수정과 다른 권한이다** — 한 권한으로 묶으면 담당 스터디를 굴리는 네비게이터가 사이트에 스터디를 세울 수 있게 된다',
        '**스터디 공지와 사이트 공지를 이름으로 가른다** — 「공지 발행」 한 줄만 두면 네비게이터가 담당 스터디에 공지를 못 올리는 것처럼 읽힌다',
        '반 편성은 스터디에 하는 일이지만 사이트 전체 표에 둔다 — 네비게이터가 못 하는 일이라 스터디 단위 표에 넣으면 그 표에서 홀로 빈 줄이 된다',
      ],
      data: ['역할별 허용 권한 집합 (한 곳에서만 정의)'],
    },
  ],
};

export const SPECS = [LIST_SPEC, ROLE_SPEC];
